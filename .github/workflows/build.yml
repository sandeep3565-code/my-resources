name: Build

on:
  push:
    branches:
      - develop
      - feature/*
  pull_request:
    branches:
      - develop
      - master

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout source
        uses: actions/checkout@v4

      - name: Show environment
        run: |
          uname -a
          gcc --version || true

      - name: Build (placeholder)
        run: |
          echo "Running build..."
          # put your real build command here
          # example:
          # make all

